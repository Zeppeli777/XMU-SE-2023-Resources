LasVegas(x):
    while True:
        a = A(x)  // 调用偏真算法 A
        b = B(x)  // 调用偏假算法 B
        
        if a == "是" 且 b == "是":
            // A 说“是”，B 也说“是” → 仅当真实为“是”（因 B 对“否”实例高概率说“否”，若真实“否”，B 说“是”概率 ≤ 1-q < 1/2 ）
            return "是"  
        
        if a == "否" 且 b == "否":
            // A 说“否”，B 也说“否” → 仅当真实为“否”（因 A 对“是”实例高概率说“是”，若真实“是”，A 说“否”概率 ≤ 1-p < 1/2 ）
            return "否"  
        
        // 否则（A 和 B 结果矛盾，说明至少一个算法出错，重试）